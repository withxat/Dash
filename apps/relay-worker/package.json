{
  "name": "@dash/relay-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dash relay — OAuth callback bridge and Cloudflare→APNs push forwarder.",
  "scripts": {
    "deploy": "wrangler deploy",
	"lint": "eslint src",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "catalog:devtool",
    "wrangler": "^4.0.0"
  }
}
