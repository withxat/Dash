{
  "name": "@dash/relay-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dash OAuth relay — converts Cloudflare's https OAuth callback into the dash:// redirect the iOS app captures.",
  "scripts": {
    "deploy": "wrangler deploy",
	"lint": "eslint src",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "catalog:devtool",
    "wrangler": "^4.0.0"
  }
}
