# BlossomColorPicker

Vendored from [Lakr233/BlossomColorPicker](https://github.com/Lakr233/BlossomColorPicker) (tag `1.0.0`), MIT License.

`Package.swift` exposes `BlossomColorPickerCore` as a product so Dash can embed `ExpandedBlossomView` instead of the stock collapsed-swatch presenter.
